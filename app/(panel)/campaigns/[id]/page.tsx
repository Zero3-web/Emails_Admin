import { notFound } from "next/navigation"; import Link from "next/link"; import { PageHeader, SiteMark, StatusBadge } from "@/src/components/ui"; import { campaigns, properties, siteById } from "@/src/data/mock";
export default async function CampaignDetail({params}:{params:Promise<{id:string}>}){const {id}=await params;const c=campaigns.find(x=>x.id===id);if(!c)notFound();const s=siteById(c.siteId)!;return <><PageHeader eyebrow="Detalle de campaña" title={c.name} description={c.subject} action={<div style={{display:"flex",gap:8}}><button className="btn">Enviar prueba</button><button className="btn primary">Preparar envío</button></div>}/><div className="settings-grid"><div><div className="card form-card"><div className="section-head"><span className="brand-cell"><SiteMark site={s}/><span><strong>{s.name}</strong><br/><small className="muted">{s.senderEmail}</small></span></span><StatusBadge status={c.status}/></div><div className="form-grid"><div><span className="muted">Automatización</span><p>{c.automationType}</p></div><div><span className="muted">Destinatarios</span><p>{c.recipientCount.toLocaleString("es-PE")}</p></div><div><span className="muted">Programación</span><p>{c.scheduledAt??"Sin programar"}</p></div><div><span className="muted">Enviada</span><p>{c.sentAt??"—"}</p></div></div>{c.errorMessage&&<p style={{background:"#ffedeb",color:"#b42318",padding:12,borderRadius:8}}>{c.errorMessage}</p>}</div><section className="section"><h2 className="section-title">Contenido de la campaña</h2><div className="card">{properties.filter(p=>p.siteId===s.id).slice(0,3).map(p=><div className="status-line" style={{padding:14}} key={p.id}><span><strong>{p.title}</strong><br/><small className="muted">{p.location} · {p.area} m²</small></span><span>USD {p.price.toLocaleString()}</span></div>)}</div></section></div><aside><div className="card form-card"><h2 className="section-title">Acciones</h2><Link className="btn" href={`/templates?site=${s.id}`} style={{width:"100%",marginTop:14}}>Abrir preview</Link><button className="btn danger" style={{width:"100%",marginTop:9}}>Cancelar campaña</button></div></aside></div></>}
