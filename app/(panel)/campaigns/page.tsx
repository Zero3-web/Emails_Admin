import Link from "next/link"; import { PageHeader, SiteMark, StatusBadge } from "@/src/components/ui"; import { campaigns,siteById } from "@/src/data/mock";
const labels={weekly_new_properties:"Nuevas propiedades",monthly_properties:"Catálogo mensual",monthly_blog:"Blog mensual"};
export default function Campaigns(){return <><PageHeader eyebrow="Historial de envíos" title="Campañas" description="Consulta, prepara y revisa todas las comunicaciones."/><div className="card form-card" style={{marginBottom:16,padding:14}}><div style={{display:"flex",gap:10,flexWrap:"wrap"}}><select aria-label="Marca"><option>Todas las marcas</option></select><select aria-label="Tipo"><option>Todos los tipos</option></select><select aria-label="Estado"><option>Todos los estados</option></select><input type="date" aria-label="Fecha" style={{maxWidth:180}}/></div></div><div className="card table-wrap"><table><thead><tr><th>Marca</th><th>Campaña</th><th>Tipo</th><th>Fecha</th><th>Destinatarios</th><th>Estado</th></tr></thead><tbody>{campaigns.map(c=>{const s=siteById(c.siteId)!;return <tr key={c.id}><td><span className="brand-cell"><SiteMark site={s} small/>{s.name}</span></td><td><Link href={`/campaigns/${c.id}`}><strong>{c.name}</strong></Link></td><td>{labels[c.automationType]}</td><td>{c.sentAt??c.scheduledAt??"—"}</td><td>{c.recipientCount.toLocaleString("es-PE")}</td><td><StatusBadge status={c.status}/></td></tr>})}</tbody></table></div></>}
